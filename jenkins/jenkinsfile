#!/usr/bin/env groov
pipeline {
    agent any
    stages {
        stage("Npm Install") {
            steps {
                sh "npm install"
            }
        }
        stage("Npm Build") {
            steps {
                sh "npm run build"
            }
        }
    }
}
